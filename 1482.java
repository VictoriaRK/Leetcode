class Solution {
    public int minDays(int[] bloomDay, int m, int k) {
        // make m bouquets
        // k adjacent flowers from garden
        if (bloomDay.length < m*k){
            return -1;
        }

        int[] spots = new int[bloomDay.length];
        
    }
}
